# Copy this file to .env for local development.
# On Render, these are set in the dashboard instead (see README).

DATABASE_URL=postgres://user:password@localhost:5432/reststop
SESSION_SECRET=replace-with-a-long-random-string
NODE_ENV=development
PORT=3000
